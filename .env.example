# 敏感配置模板（复制为 .env 并填入真实值，.env 不入库）
# 权限建议：chown root:www && chmod 640
APP_ADMIN_USER="admin"
# APP_ADMIN_PASS 存 password_hash() 生成的 hash（生成：php -r "echo password_hash('密码', PASSWORD_DEFAULT);"）
APP_ADMIN_PASS=""
APP_GEMINI_KEY=""
APP_OPENAI_KEY=""
